#configuration info
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssm_db?serverTimezone=UTC
    username: root
    password: root

#set MyBatis Plus
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tbl_
